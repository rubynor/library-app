<div class="bg-blue-500">
    <div class="drawer lg:drawer-open">
        <input id="my-drawer" type="checkbox" class="drawer-toggle" />
        <div class="drawer-content bg-white h-full lg:rounded-l-2xl">
            <%= yield %>
        </div>
        <div class="drawer-side">
            <label for="my-drawer" aria-label="close sidebar" class="drawer-overlay"></label>
            <ul class="menu bg-blue-500 text-3xl px-12 font-semibold flex items-start gap-8 min-h-full w-80 p-4 text-white">
                <li>
                    <div class="py-28">
                        <img alt="rubynor logo hvite forenklet" src="<%= asset_path('Rubynor_logo_forenklet_hvit.png') %>" width="300" />
                    </div>
                </li>
                <li class="w-full flex items-center">Home</li>
                <li class="w-full flex items-center">
                    <div class="indicator">
                        <div class="indicator-item indicator-bottom">
                            <button class="btn btn-primary">Coming soon</button>
                        </div>
                        <div>
                            <p>Wishlist</p>
                        </div>
                    </div>
                </li>
                <%# <li class="w-full flex items-center">Account</li> %>
                <%# <li class="w-full flex items-center">Settings</li> %>
            </ul>
        </div>
    </div>
</div>



